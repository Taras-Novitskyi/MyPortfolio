export const selectError = (state: any) => state.user.error;

export const selectIsLoading = (state: any) => state.auth.isLoading;

export const selectMotivation = (state: any) => state.user.isShowModal;
